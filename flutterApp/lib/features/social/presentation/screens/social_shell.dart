import 'package:flutter/material.dart';

class SocialShell extends StatelessWidget {
  final Widget child;

  const SocialShell({super.key, required this.child});

  @override
  Widget build(BuildContext context) {
    return child;
  }
}
